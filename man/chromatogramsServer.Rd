% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/observers_chromatograms.R,
%   R/observers_chromatogramsOverlay.R, R/outputs_chromatograms.R
\name{chromatogramsServer}
\alias{chromatogramsServer}
\alias{base_chromatograms}
\alias{slider}
\alias{nxt}
\alias{prv}
\alias{zoom_chromatograms}
\alias{dblclick_chromatograms}
\alias{dblclick_chromatogramsOverlay}
\title{Server Logic for Chromatograms Plot}
\usage{
slider(input, output, session, object_reactive, i, id = "chromatogramsPlot")

nxt(input, output, session, object_reactive, i, id = "chromatogramsPlot")

prv(input, output, session, object_reactive, i, id = "chromatogramsPlot")

zoom_chromatograms(input, output, session, id = "chromatogramsPlot")

dblclick_chromatograms(
  input,
  output,
  session,
  object_reactive,
  id = "chromatogramsPlot"
)

dblclick_chromatogramsOverlay(
  input,
  output,
  session,
  object_reactive,
  id = "chromatogramsOverlayPlot"
)

base_chromatograms(
  input,
  output,
  session,
  object_reactive,
  i,
  id = "chromatogramsPlot"
)
}
\arguments{
\item{input}{Shiny input object}

\item{output}{Shiny output object}

\item{session}{Shiny session object}

\item{object_reactive}{Shiny reactive object with inside Chromatograms object}

\item{i}{Shiny reactive object with the index of the spectra currently
visualized}

\item{id}{\code{character(1)} with the ID of the namespace of the input.}
}
\description{
Server-side observers for the chromatograms plot display.
Handles reactive updates to the plot and data table when user inputs change.

Update the figure based on the slider value.

Update the plot with the next spectrum.

Update the plot with the previous spectrum.

Zoom the plot with brush

Zoom/unzoom based on double click

Zoom/unzoom based on double click

The function contains all the shiny functions that update the \code{output} shiny
variable of the Chromatograms plot. It is included alse the function to
download the plot and the filtered RDS file.
}
\details{
This module contains multiple \code{observeEvent} handlers that update the plot
and data table when the user modifies:
\itemize{
\item Slider position for spectrum browsing
\item Navigation buttons (previous/next spectrum)
\item Zoom and double click on the plot
}
}
\author{
Gabriele Tomè
}
\keyword{internal}
