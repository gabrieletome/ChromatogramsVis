% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/observers_input.R
\name{inputServer}
\alias{inputServer}
\alias{input_cat}
\alias{load_r_obj}
\alias{load_raw_file}
\alias{load_rds_file}
\alias{load_galaxy}
\title{Server Logic for input methods}
\usage{
input_cat(input, output, session, object_reactive)

load_r_obj(input, output, session, object, object_reactive)

load_raw_file(input, output, session, object_reactive)

load_rds_file(input, output, session, object_reactive)

load_galaxy(input, output, session, object_reactive)
}
\arguments{
\item{input}{Shiny input object}

\item{output}{Shiny output object}

\item{session}{Shiny session object}

\item{object_reactive}{Shiny reactive object with inside Chromatograms object}

\item{object}{\code{Chromatograms} or \code{MsExperiment} object passed via R console}
}
\description{
Handle the serve side of the input methods. Load data from various sources
(R console, raw files, RDS objects, or Galaxy history), build a
Chromatograms object and generate the GUI for the visualization.

Clean the GUI if the input radioButton change

Check if the object is a \code{Chromatograms} or \code{MsExperiment}. If it is a
\code{MsExperiment} convert to \code{Chromatograms} based on the summarized method
selected. If \code{"In memory"} option is selected it change the backend to
\code{ChromBackendMemory}. Activate the GUIs.

If \code{"In memory"} option is selected read the file as \code{ChromBackendMemory},
otherwise as \code{ChromBackendMzR}. Activate the GUIs.

Check if the object inside the \code{"RDS file"} is a \code{Chromatograms} or
\code{MsExperiment}. If it is a \code{MsExperiment} convert to \code{Chromatograms} based
on the summarized method selected. If \code{"In memory"} option is selected it
change the backend to \code{ChromBackendMemory}. Activate the GUIs.

Read the Galaxy history and based on the input file read it. Check if the
object is a \code{Chromatograms} or \code{MsExperiment}. If it is a \code{MsExperiment}
convert to \code{Chromatograms} based on the summarized method selected. If
\code{"In memory"} option is selected it change the backend to
\code{ChromBackendMemory}. Activate the GUIs.
}
\details{
Based on the input:
\itemize{
\item Load the data
\item If not a \code{Chromatograms} object, build it
\item If \code{"In memory"} option is selected it change the backend to
\code{ChromBackendMemory}
\item Activete the GUIs
}
}
\author{
Gabriele Tomè
}
\keyword{internal}
